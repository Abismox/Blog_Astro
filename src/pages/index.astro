---
// Redirect to default language (Spanish).
// Use import.meta.env.BASE_URL so this works under a project-site base path
// like /Blog_Astro (configured in astro.config.mjs).
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
return Astro.redirect(`${base}/es`);
---